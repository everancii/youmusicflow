---
phase: 01-reliable-core
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/client/controls.ts, src/main.ts, spec/client/controls.spec.ts]
autonomous: true
requirements: [CTRL-01, TRAY-01]

must_haves:
  truths:
    - "Prev/next work via player-bar buttons even when the queue panel is not rendered"
    - "Prev/next do not silently fail at queue boundaries"
    - "macOS tray icon swaps light/dark immediately when system theme changes, no restart"
  artifacts:
    - path: "src/client/controls.ts"
      provides: "Player-bar-first prev/next with ordered fallbacks"
      contains: "previous-button"
    - path: "src/main.ts"
      provides: "nativeTheme updated listener re-setting tray image"
      contains: "nativeTheme.on('updated'"
    - path: "spec/client/controls.spec.ts"
      provides: "Coverage for player-bar selectors and fallback order"
      contains: "previous-button"
  key_links:
    - from: "src/main.ts"
      to: "src/tools/getNativeIconName.ts"
      via: "theme listener reuses getNativeIconName() for the new image path"
      pattern: "nativeTheme\\.on\\('updated'"
---

<objective>
Make prev/next controls robust via YT Music's player-bar buttons, and make the macOS tray icon react to theme changes at runtime.

Purpose: Controls that always work + tray icon that never looks wrong (CTRL-01, TRAY-01).
Output: Rewritten prevTrack/nextTrack with ordered selector fallbacks + tests; nativeTheme listener updating tray image in main.ts.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reliable-core/RESEARCH.md
@src/client/controls.ts
@src/main.ts
@spec/client/controls.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Player-bar-first prev/next in controls.ts</name>
  <files>src/client/controls.ts</files>
  <read_first>src/client/controls.ts, spec/client/controls.spec.ts</read_first>
  <action>Rewrite `prevTrack(document)` and `nextTrack(document)` with ordered fallback (per RESEARCH.md): 1) `document.querySelector('ytmusic-player-bar .previous-button')` (`.next-button` for next), 2) unscoped `document.querySelector('.previous-button')`, 3) existing queue-walk logic as last resort (keep it, extracted or inline). First selector that yields an element → `.click()` it → return. Preserve the existing function signatures and `document: any` parameter convention. Keep `playPause` unchanged. No semicolons, single quotes.</action>
  <verify>npm test -- spec/client/controls.spec.ts</verify>
  <acceptance_criteria>
    - prevTrack/nextTrack query 'ytmusic-player-bar .previous-button' / '.next-button' first
    - Queue-walk logic retained as final fallback
    - Existing controls tests still pass
  </acceptance_criteria>
  <done>Prev/next click player-bar buttons when present, regardless of queue panel state</done>
</task>

<task type="auto">
  <name>Task 2: Tests for selector fallback order</name>
  <files>spec/client/controls.spec.ts</files>
  <read_first>spec/client/controls.spec.ts</read_first>
  <action>Add JSDOM fixtures + cases: (a) player-bar markup present (`<ytmusic-player-bar><button class="previous-button"/><button class="next-button"/></ytmusic-player-bar>`) → bar button clicked, queue untouched; (b) bar absent, queue present → queue-walk fallback still clicks sibling item (existing behavior); (c) neither present → no throw. Follow existing spec structure (JSDOM constructed per test, click spies via addEventListener or jest.fn on element.click).</action>
  <verify>npm test -- spec/client/controls.spec.ts</verify>
  <acceptance_criteria>
    - New tests cover player-bar primary path for both prev and next
    - Fallback-to-queue case covered
    - Full suite green
  </acceptance_criteria>
  <done>Selector priority locked by tests; regressions in fallback order will fail CI</done>
</task>

<task type="auto">
  <name>Task 3: Live tray theme reaction in main.ts</name>
  <files>src/main.ts</files>
  <read_first>src/main.ts, src/tools/getNativeIconName.ts</read_first>
  <action>After tray creation in the ready handler, register: `nativeTheme.on('updated', () => { if (isMacOS()) tray.setImage(path.join(<existing assets path expr>, getNativeIconName())) })` — reuse the exact same path expression used to build the initial tray image, and the existing `nativeTheme` import (add to the electron import list if absent). Guard with the same platform check pattern main.ts already uses (platformResolver `isMacOS()` or equivalent existing helper). NOTE: 01-01 already modified main.ts — rebase on its result, keep edits surgical.</action>
  <verify>npm run build && grep -n "nativeTheme.on" app/main.js</verify>
  <acceptance_criteria>
    - main.ts registers nativeTheme 'updated' listener after tray creation
    - Listener calls tray.setImage with getNativeIconName() result
    - Non-macOS guarded (no setImage churn on Windows/Linux)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Toggling macOS appearance flips tray icon immediately</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] npm test passes including new controls cases
- [ ] grep confirms all acceptance criteria patterns present
</verification>

<success_criteria>
- All tasks completed
- CTRL-01: prev/next reliable without queue panel, no boundary dead-ends
- TRAY-01: tray icon live-follows macOS theme
- playPause behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-reliable-core/01-02-SUMMARY.md`
</output>
