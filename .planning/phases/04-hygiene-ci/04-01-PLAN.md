---
phase: 4
plan: 04-01
wave: 1
depends_on: []
requirements: [HYG-01, HYG-02, HYG-03, HYG-04]
must_haves:
  truths:
    - npm install + npm run build + npm test green after dep removals
    - Single lockfile (package-lock.json); yarn.lock gone
    - One CI system — GitHub Actions matrix (mac/win/linux); CircleCI + AppVeyor configs deleted
    - AGENTS.md describes actual code (no auto-update, no ad-block, no S3; current module layout)
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - package.json
    - AGENTS.md
---

# Plan 04-01: Dependency Cleanup + GitHub Actions + Honest Docs

## Objective
One lockfile, one CI, zero dead dependencies, docs matching reality.

## Context
- RESEARCH.md: dead deps = `@types/semver`, `electron-log`, `codecov`, `standard-version`. yarn.lock stale. AGENTS.md false claims at L27/71/73/76 + stale architecture tree.

## Tasks

### Task 1: Dependencies + lockfile
- `npm uninstall @types/semver electron-log codecov standard-version` (updates package.json + package-lock.json).
- Delete `yarn.lock` and `codecov.yml`.
- Verify: `npm run build` + `npm test` green.

### Task 2: GitHub Actions CI
- Create `.github/workflows/ci.yml`: name CI; on push (main) + pull_request; matrix os [ubuntu-latest, macos-latest, windows-latest]; steps: actions/checkout@v4 → actions/setup-node@v4 (node 22, cache npm) → `npm ci` → `npm run build` → `npm test`.
- Delete `.circleci/config.yml` (and empty `.circleci/`) + `appveyor.yml` in the same commit.

### Task 3: AGENTS.md rewrite
Update to match reality:
- Architecture tree: main.ts orchestrator + `main-process/` modules (createMainWindow, createTray, createSettingsWindow, registerShortcuts, registerIpc, positionWindow, nowPlaying), `tools/navigationPolicy`, `src/offline/offline.html`, settings preload/contextIsolation, client preload (media keys + appShell.retry + mediaSession poll).
- Remove: auto-update.ts references, ad/tracker-blocking claim, S3 publishing claim.
- Update gotchas: copy-assets handles settings + offline groups; window resizable with persisted `windowSize` (main-only setting); blur-hide respects alwaysOnTop; settings window is contextIsolation:true via preload.
- Commands table: add CI note (GitHub Actions). Testing section: mention spec/main-process/, snapshot for IPCEventNames includes NOW_PLAYING.
- Keep true content (offsetCalclator typo warning, prettier style, electron-store key name).

## Acceptance
- `npm run build` + `npm test` green; `git grep -l "circleci\|appveyor\|codecov" -- ':!node_modules' ':!.planning'` → empty (except historical planning docs).
- No `yarn.lock`; `.github/workflows/ci.yml` valid YAML.
- AGENTS.md contains `main-process/` and no `auto-update`/ad-block/S3 claims.
- Conventional commits (chore: deps/lockfile, ci: workflow swap, docs: AGENTS.md).
