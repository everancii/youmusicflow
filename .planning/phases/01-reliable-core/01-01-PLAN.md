---
phase: 01-reliable-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main.ts, src/tools/settings.ts, src/settings/index.html, src/settings/renderer.js]
autonomous: true
requirements: [INST-01, CTRL-02]

must_haves:
  truths:
    - "Launching the app a second time quits the new instance and shows/focuses the existing window"
    - "Turning the media-keys setting off releases MediaPlayPause/MediaPreviousTrack/MediaNextTrack without restart"
    - "Turning the setting back on re-registers all three media keys without restart"
  artifacts:
    - path: "src/main.ts"
      provides: "Single-instance lock + conditional media-key registration"
      contains: "requestSingleInstanceLock"
    - path: "src/tools/settings.ts"
      provides: "enableMediaKeys setting with default true"
      contains: "enableMediaKeys"
    - path: "src/settings/index.html"
      provides: "Media keys checkbox"
      contains: "enable-media-keys"
  key_links:
    - from: "src/settings/renderer.js"
      to: "src/main.ts"
      via: "update-setting IPC with enableMediaKeys key"
      pattern: "update-setting.*enableMediaKeys|enableMediaKeys.*update-setting"
    - from: "src/main.ts"
      to: "src/tools/settings.ts"
      via: "getSetting('enableMediaKeys') gates registration"
      pattern: "getSetting\\('enableMediaKeys'\\)"
---

<objective>
Add single-instance lock and a live media-keys toggle setting.

Purpose: One running app owning media keys — and only when the user wants it to (INST-01, CTRL-02).
Output: Locked instancing in main.ts; new enableMediaKeys setting end-to-end (store schema → settings UI → live register/unregister).
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reliable-core/RESEARCH.md
@src/main.ts
@src/tools/settings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-instance lock in main.ts</name>
  <files>src/main.ts</files>
  <read_first>src/main.ts</read_first>
  <action>At module top level (after imports, before `app.on('ready')`): call `const gotTheLock = app.requestSingleInstanceLock()`. If false → `app.quit()` and skip registering the ready handler (wrap existing `app.on('ready', ...)` and other listeners in `if (gotTheLock) { ... }` or early-pattern equivalent — keep diff minimal: `if (!gotTheLock) { app.quit() } else { ...existing listeners... }`). Inside the else branch add `app.on('second-instance', () => { if (mainWindow) { mainWindow.show(); mainWindow.focus() } })`. Code style: no semicolons, single quotes (Prettier).</action>
  <verify>npm run build && grep -n "requestSingleInstanceLock" app/main.js</verify>
  <acceptance_criteria>
    - src/main.ts contains "requestSingleInstanceLock" before the ready handler
    - src/main.ts contains a "second-instance" listener that shows and focuses mainWindow with a null guard
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Second launch quits itself; first instance window is shown/focused via second-instance event</done>
</task>

<task type="auto">
  <name>Task 2: enableMediaKeys setting in store and settings UI</name>
  <files>src/tools/settings.ts, src/settings/index.html, src/settings/renderer.js</files>
  <read_first>src/tools/settings.ts, src/settings/index.html, src/settings/renderer.js</read_first>
  <action>settings.ts: add `enableMediaKeys: boolean` to the AppSettings interface, add schema entry `{ type: 'boolean', default: true }`, and include it in getSettings() return (match existing @ts-ignore pattern — full typing lands in Phase 2, do not refactor now). index.html: add a new `.setting-item` div with label "Global media keys" and `<input type="checkbox" id="enable-media-keys">`, placed after the always-on-top item — copy that item's exact markup structure. renderer.js: initialize `document.getElementById('enable-media-keys').checked = settings.enableMediaKeys !== false` and add change listener sending `ipcRenderer.send('update-setting', 'enableMediaKeys', e.target.checked)` — mirror the always-on-top listener pattern exactly (CommonJS file, keep existing style).</action>
  <verify>npm run build && grep -n "enable-media-keys" app/settings/index.html app/settings/renderer.js && grep -n "enableMediaKeys" app/tools/settings.js</verify>
  <acceptance_criteria>
    - settings.ts schema contains "enableMediaKeys" with "default: true"
    - index.html contains id="enable-media-keys"
    - renderer.js sends 'update-setting' with 'enableMediaKeys'
    - Build copies updated settings assets into app/settings/
  </acceptance_criteria>
  <done>Setting persists via electron-store, checkbox appears in settings window, checked by default</done>
</task>

<task type="auto">
  <name>Task 3: Conditional registration + live toggle in main.ts</name>
  <files>src/main.ts</files>
  <read_first>src/main.ts, .planning/phases/01-reliable-core/RESEARCH.md</read_first>
  <action>Extract the three `globalShortcut.register` calls into a local function `registerMediaKeys()` and add `unregisterMediaKeys()` that calls `globalShortcut.unregister` for 'MediaPlayPause', 'MediaPreviousTrack', 'MediaNextTrack' individually (NOT unregisterAll). At ready: call `registerMediaKeys()` only when `getSetting('enableMediaKeys') !== false`. In the existing `ipcMain.on('update-setting', ...)` handler add: `if (key === 'enableMediaKeys') { value ? registerMediaKeys() : unregisterMediaKeys() }`. Guard registerMediaKeys against double registration with `globalShortcut.isRegistered('MediaPlayPause')` early return.</action>
  <verify>npm run build && grep -n "unregisterMediaKeys\|isRegistered" app/main.js</verify>
  <acceptance_criteria>
    - src/main.ts contains "getSetting('enableMediaKeys')" gating registration
    - src/main.ts update-setting handler branches on 'enableMediaKeys' calling register/unregister functions
    - unregister uses individual accelerators, NOT globalShortcut.unregisterAll
  </acceptance_criteria>
  <done>Toggling the checkbox registers/releases media keys immediately, no restart</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] npm test passes (existing suites unaffected)
- [ ] grep confirms all acceptance criteria patterns present
</verification>

<success_criteria>
- All tasks completed
- INST-01: second launch focuses existing instance
- CTRL-02: media keys toggleable live, default enabled
- No behavior change for users who never touch the new setting
</success_criteria>

<output>
After completion, create `.planning/phases/01-reliable-core/01-01-SUMMARY.md`
</output>
